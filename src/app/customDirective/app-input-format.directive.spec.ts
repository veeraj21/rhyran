import { AppInputFormatDirective } from './app-input-format.directive';

describe('AppInputFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AppInputFormatDirective();
    expect(directive).toBeTruthy();
  });
});
